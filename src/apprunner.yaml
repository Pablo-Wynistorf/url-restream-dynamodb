version: 1.0
runtime: nodejs18
build:
  commands:    
    build:
      - npm install                                 
run:                              
  command: node express.js
  network:
    port: 80
